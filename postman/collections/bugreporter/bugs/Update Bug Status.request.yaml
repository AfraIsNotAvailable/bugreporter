$kind: http-request
name: Update Bug Status
url: "{{bugs}}/1/status?status=OPEN"
method: PATCH
queryParams:
  status: OPEN
auth:
  type: bearer
  credentials:
    token: "{{jwt_token}}"
order: 1775141554882
